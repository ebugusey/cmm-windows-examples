Workaraounds for issue at https://github.com/VorpalBlade/chezmoi_modify_manager/issues/253

Switch branches to see examples:
* `easy-way` -- using CMM as [interpreter](https://www.chezmoi.io/reference/configuration-file/interpreters/) on windows to execute `modify_` script.
* `involved-example` -- using symlinks and git-for-windows `sh` magic (solution I'm currently using)
